main (
    asnrank integer,#rank
    asn character(50),#isp number
    asname character(200),#isp name/code
    country_code character(2),
    users integer,
    gdpxusers real,#gdp
    v6users real,
    v6percentageofas real,
    samples bigint
)

countries
(
      name text,
      date date,
    #wazimap id's
      geo_code character(2),#country code
      geo_level text,
      parent_level text,
      geo_version text
    #our data
      rank integer,#rated by number users
      population bigint,#needs to be added in from other database
      users bigint,
      gdpxusers real,
      v6users bigint,
      v6percentageofas real,
      samples bigint,
)

asn_to_name
(
      asn text,
      name text,
      trading_name text
)

users_in_country
(
    geo_level text,
    geo_code character(2),
    geo_version text,
    user_or_not text,
    total bigint;
)

users_in_world
(
    geo_level text,
    geo_code character(2),
    geo_version text,
    country_or_world text,#users_in_country or w_users
    total bigint;#user in the country or (total users in the world - users_in_country)
)

market_share(
    geo_level text,
    geo_code character(2),
    geo_version text,
    asn text,
    number_of_user bigint
)

v6users_in_country
(
    geo_level text,
    geo_code character(2),
    geo_version text,
    user_or_not text,
    total bigint;
)

v6users_in_world
(
    geo_level text,
    geo_code character(2),
    geo_version text,
    v6coutry_or_world text,
    total bigint;
)

v6market_share(
    geo_level text,
    geo_code character(2),
    geo_version text,
    v6asn text,
    number_of_v6user bigint
)
